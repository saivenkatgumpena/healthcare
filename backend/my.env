PORT=5000
# Paste YOUR password only (no < >). Or paste the full SRV string from Atlas: Connect → Drivers.
# If password has @ # : / ? encode them (e.g. @ → %40).
# Cloud connection string (replace REPLACE_ME with your actual password):
# MONGO_URI=mongodb+srv://saivenkat:REPLACE_ME@cluster0.quhrbdw.mongodb.net/mern_hospital?retryWrites=true&w=majority&appName=Cluster0

# Currently attempting a local MongoDB connection for easiest setup:
MONGO_URI=mongodb://127.0.0.1:27017/mern_hospital
JWT_SECRET_KEY=hms_local_jwt_secret_min_32_chars_long_2026
JWT_EXPIRES=7d
COOKIE_EXPIRE=7
FRONTEND_URL_ONE=http://localhost:5173
FRONTEND_URL_TWO=http://localhost:5174

# Optional for doctor image uploads
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# SMTP Email Setup for Notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SERVICE=gmail
SMTP_MAIL=doctorsarah618@gmail.com
SMTP_PASSWORD=uwzyjzefcajpqyuw